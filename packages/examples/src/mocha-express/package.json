{
    "name": "example-mocha-express",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "test": "cross-env NODE_ENV=test mocha --no-color ./__tests__/expressApp.test.js"
    },
    "devDependencies": {
        "chilldoc": "workspace:*",
        "cross-env": "^7.0.3",
        "mocha": "workspace:*"
    }
}
